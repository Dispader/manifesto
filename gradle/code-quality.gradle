apply plugin: 'codenarc'

codenarc {
    configFile = file('src/check/codenarc/codenarc.groovy')
}

tasks.withType(CodeNarc) {
    reports {
        xml.enabled = true
        html.enabled = true
    }
}

codenarcTest {
    exclude '*'
}
