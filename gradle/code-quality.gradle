apply plugin: 'codenarc'

codenarc {
    toolVersion = '1.2.1'
    configFile = file('src/check/codenarc/codenarc.groovy')
    sourceSets = [sourceSets.main]
    ignoreFailures = true
}
codenarcMain {
    group = 'Code Quality'
    reports {
        xml.enabled = true
        html.enabled = true
    }
}
